# NOETARCH frontend configuration. Copy to .env.local for local dev.
#
# Base URL of the backend API. When UNSET, the app runs standalone on mock data (no network).
# When set, screens fetch from the real backend. The one-command dev path sets this for you.
NEXT_PUBLIC_API_BASE=http://localhost:8000
